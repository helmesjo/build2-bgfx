# Match libbgfx: C++20 avoids Apple SDK + C++26 Objective-C++ issues.
#
cxx.std = c++20

using cxx
using cxx.objcxx

# 42-bunnylod/progmesh.c is plain C, called from bunnylod.cpp via extern "C".
#
using c

hxx{*}: extension = h
ixx{*}: extension = ipp
txx{*}: extension = tpp
cxx{*}: extension = cpp

# Required when including libbx public headers under MSVC.
#
if ($cxx.target.system == 'win32-msvc')
  cxx.coptions += /Zc:preprocessor

test.target = $cxx.target
